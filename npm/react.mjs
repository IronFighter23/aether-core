// React subpath entry: ``import { useAether } from '@ironfighter23/aether-core/react'``
//
// This module is ESM-only (React 18+ projects use ESM by default;
// older CRA setups handle .mjs via their Webpack config). If you
// need a CommonJS React build, open an issue and we'll add one --
// no consumer has needed it yet.

export {
    useAether,
    useAetherSnapshot,
    useAetherSupersede,
    configureAether,
    getAether,
} from './aether-react.mjs';

export { default } from './aether-react.mjs';
